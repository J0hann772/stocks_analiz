import asyncio; import pandas as pd; from services.fmp_client import fmp_client; async def run(): data = await fmp_client.get_historical_chart('AAPL', '1min', '2025-12-10', '2026-03-10'); df = pd.DataFrame(data); df['date'] = pd.to_datetime(df['date']); df.set_index('date', inplace=True); df.sort_index(inplace=True); print(df.loc['2026-03-09 10:00:00':'2026-03-09 15:59:00']['low'].min()); print(df.loc['2026-03-10 10:00:00':'2026-03-10 15:59:00']['low'].min()); asyncio.run(run())
